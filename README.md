# 2026 World Cup Album Sticker Manager

### Easy initialization
Use the following command to start the app:

``` bash
    npm run start
```

The script will:
- Create the database
- Start the app at localhost:3000